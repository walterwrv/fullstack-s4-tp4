<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Formulario Superhéroes</title>
  <style>
   
  </style>
</head>
<body>
  <div class="form-container">
    <h1>Registrar Superhéroe</h1>
    <!-- El formulario ahora se enviará a la ruta POST '/superheroes/guardar' -->
    <form id="superheroForm" method="POST" action="./guardar">


      <!-- Mostrar los errores -->
      <% if (errors.length > 0) { %>
          <ul style="color: red;">
              <% errors.forEach(error => { %>
                  <li><%= error.msg %></li>
              <% }) %>
          </ul>
      <% } %>

      <div class="form-group">
        <label for="nombreSuperHeroe">Nombre del Superhéroe</label>
        <input type="text" id="nombreSuperHeroe" name="nombreSuperHeroe" value="<%= datosHeroe.nombreSuperHeroe %>" required>
      </div>
      <div class="form-group">
        <label for="nombreReal">Nombre Real</label>
        <input type="text" id="nombreReal" name="nombreReal" value="<%= datosHeroe.nombreReal %>" required>
      </div>
      <div class="form-group">
        <label for="edad">Edad</label>
        <input type="number" id="edad" name="edad" min="0" value="<%= datosHeroe.edad %>" required>
      </div>
      <div class="form-group">
        <label for="planetaOrigen">Planeta de Origen</label>
        <input type="text" id="planetaOrigen" name="planetaOrigen" value="<%= datosHeroe.planetaOrigen %>" required>
      </div>
      <div class="form-group">
        <label for="debilidad">Debilidad</label>
        <input type="text" id="debilidad" name="debilidad" value="<%= datosHeroe.debilidad %>" required>
      </div>
      <div class="form-group">
        <label for="poderes">Poderes (separados por comas)</label>
        <textarea id="poderes" name="poderes" required><%= datosHeroe.poderes %></textarea>
      </div>
      <div class="form-group">
        <label for="aliados">Aliados (separados por comas)</label>
        <textarea id="aliados" name="aliados" required><%= datosHeroe.aliados %></textarea>
      </div>
      <div class="form-group">
        <label for="enemigos">Enemigos (separados por comas)</label>
        <textarea id="enemigos" name="enemigos"  required><%= datosHeroe.enemigos %></textarea>
      </div>
      <div class="form-group">
        <label for="creador">Cargado por</label>
        <input type="text" id="creador" value="<%= datosHeroe.creador %>" name="creador" >
      </div>
      <div class="form-group">
        <button type="submit">Guardar Superhéroe</button>
      </div>
    </form>
  </div>
</body>
</html>